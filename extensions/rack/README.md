# rack

Please refer to the [reference page](https://docs.infrahub.app/schema-library/reference/rack) for the corresponding documentation.

`LocationSite` comes from either `extensions/location_minimal` or `extensions/location_site`. `LocationRack` is also defined, hierarchically and incompatibly, by `experimental/location_extended`, so load one or the other, never both.
